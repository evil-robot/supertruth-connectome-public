# Decision log

Dated rulings on the open questions in PROTOCOL.md §12. "Default" = set by the build lead under stated assumptions; JAS may overrule.

| Date | Q | Ruling | By | Reason / cost if wrong |
|---|---|---|---|---|
| 2026-09-20 | Q1 | Teacher = `src/lib/pipeline.ts` (deployed at supertruth.ai/api/v1/score), pinned commit recorded in teachers/MANIFEST.md. | default | Paper weights = engine weights, so the language-model arm (given the paper) is fair. |
| 2026-09-20 | Q2 | NO engine internals in the paper. DTI described only as far as the April 2026 paper does; feature spec published without regexes or constants. | counsel flag 1 + JAS rule | Proprietary; April paper is the disclosure boundary. |
| 2026-09-20 | Q3 | Generator changes land in THIS repo (teachers/), never in attested-baseline. Attested Baseline is never named or described. | counsel flag 2 | Unfiled invention. |
| 2026-09-20 | Q4 | Corpus B (knob-spanning generator built from the engine's input surface) is the PRIMARY corpus. Paper states plainly that records were generated to span every tier and every input the engine reads. Corpus A (schema-only description) is the out-of-distribution check. | default, assumption | All arms including the language model see the same records, so it is not teaching one student to the test. If JAS disallows, the claim shrinks to non-degenerate dimensions. |
| 2026-09-20 | Q5, A8 | AS_OF clock = 2026-09-20T00:00:00Z (was 2026-10-01 in the first draft). Live spot check runs 20 Sep 2026 UTC. | default | Teacher build already frozen on this date. |
| 2026-09-20 | Q6 | Live spot check uses SuperTruth's own sandbox tenant; 50 + 50 calls. Key from the house credential store at run time, never in the repo. | default | none material |
| 2026-09-20 | Q7, Q13 | Measured by the model build and the 2-seed pilot before any schedule statement. No clock promise. | engineering | 9/16 clock-promise rule. |
| 2026-09-20 | Q8 | Model string, parameters, and prices come from the claude-api skill at run time and are written into HOW_WE_RAN_THIS.md per run. | engineering | A price from memory is a fabricated number. |
| 2026-09-20 | Q9 | BII is a BLACK-BOX teacher: inputs (event window schema) and outputs (score, gate) only; wording "BII under the seed policy with empty registries". No signals, weights, or thresholds printed unless John Kheit confirms a filed serial. | counsel flag 1 | Patent clocks. |
| 2026-09-20 | Q10 | Zenodo DOI 10.5281/zenodo.19601616 resolved live (Zenodo REST) 20 Sep 2026. | citation pack | done |
| 2026-09-20 | Q11 | Attribution block drafted (paper.md, Connectome Attribution); Dina's approval is a PUBLISH GATE. | counsel | License breach otherwise. |
| 2026-09-20 | Q12 | Trained parameters are HELD (not released). Released: code (MIT), derived graph (CC BY 4.0), feature specs, protocol, decision rules, run manifests. | default, flag to JAS | Gains + readout reproduce the proprietary engine's decision surface, which Q2 withholds. Reproducibility comes from code + protocol + a held-out evaluation service if wanted later. |
| 2026-09-20 | flag 13 | Publicity-clause check run (house legal corpus: legal_gap_check imaware, MedSync; contracts_registry HS&E). No publication, publicity, press, or use-of-name clause surfaced in any executed document. The paper names no client and uses no client data. Counsel note kept: no publication-approval record exists for Zenodo deposits; this decision log is that record for this paper. | build lead | none found |
| 2026-09-20 | results framing | JAS: "if the random graph matches, elevate that finding; people may really care." Rule: if arm c (random graph, matched N, E, sign fraction) is within the pre-registered margin of arm a (fly wiring) on TEST across seeds, the HEADLINE finding of the paper, page, and post is "the substrate, not the anatomy": a fixed, sparse, signed recurrent graph with only per-edge gains trained reproduces the engine; the specific wiring adds nothing measurable. Abstract leads with it; §4 gives it its own table and figure (per-seed paired differences a−b, a−c with CIs); §6 reframes "we borrowed a brain" as the test that showed where the intelligence is not. The auditability argument stands (every connection is still inspectable) but is stated as a property of any fixed public graph, with the fly's anatomical labels as the extra. Arm d (matched MLP) then decides whether the fixed substrate has any edge over ordinary training; if d matches too, the paper says that plainly as well. Related work: echo state / liquid state / random features already cited [14–19]. | JAS + build lead | headline chosen after the seeds, never before |
| 2026-09-20 | composite head | Two composite estimates exist per arm: the directly predicted composite head, and the composite RECOMPUTED from the predicted eight dimensions with the engine's weights. The engine's definition is the weighted sum, and the direct head is unstable across neighbouring epochs (ER log: 2.80 → 1.80 → 1.77 at epochs 9–11; MLP similar). RULING: the recomputed composite is the headline composite metric; the direct head is reported beside it in every table. Disclosed: this choice was made after seed 1 was read, for the stated reason; it does not change tier accuracy or per-dimension results, which are the primary evidence. | build lead, flag to JAS | none if both are printed |
| 2026-09-20 | same-record comparison | The fly is re-scored on the exact 300 test records the model arms saw (a logged TEST re-read with reason), and evaluate.py dumps per-record absolute errors so the protocol's hierarchical bootstrap can run. | build lead | model-arm comparison otherwise on different records |
| 2026-09-20 | d' control | The small reference network (2×256) named in the protocol was not in the pilot driver; queued after the pilot as a descriptive control. | engineering | the matched-parameter MLP overfits 65 inputs and is a weak control by construction; d' guards the "fixed graph beats ordinary training" reading |
| 2026-09-21 | BII feature spec | Held from the code release pending John Kheit's ruling (BII unfiled; a dated public spec of its inputs widens the disclosure). DTI feature spec released. | counsel seat, build lead | patent clocks abroad |
| 2026-09-21 | marks footer | Nominative-use wording ("GPT-5 is a product of OpenAI ... each name is the property of its owner") replaces trademark-status assertions we cannot back (TTAB held GPT merely descriptive 4 Mar 2026; GROK owner of record unconfirmed). Applied to paper, page, release. | counsel seat | none |
| 2026-09-21 | counsel gate | JAS: "we don't need Dina or John." Counsel sign-off removed from the publish gate. Conservative wording kept by default: "proprietary" (not "patented") in the release and paper; attribution block as drafted; BII feature spec held from the code release; live-site boilerplate untouched. Any of these flips on JAS's word. Remaining gates: publish timing (JAS), Zenodo upload (JAS), flip CONNECTOME_PUBLISHED. | JAS | none |
| 2026-09-21 | data release | JAS: "release everything as long as it doesn't infect or violate my IP." Released on Zenodo (CC BY 4.0, own DOI): all 20,000 DTI synthetic records, features, ALL DTI engine labels, and the DTI-trained parameters (seed 1 arms); all 20,000 BII windows and features. HELD: BII scores and gates, BII-trained parameters, BII feature spec (BII unfiled; outputs beside the public four-signal description would let the method be reconstructed before filing). Teacher engines never released. Supersedes the "weights held" row for DTI. | JAS | BII prior-art self-disclosure |
| 2026-09-21 | public code snapshot | teachers/bii_features_spec.md and teachers/gen_bii.py (encode_features) are in this repository's history, and DECISIONS holds the BII spec. Ruling: the PUBLIC code release is a clean export (git archive of HEAD minus teachers/bii_features_spec.md, teachers/gen_bii.py, quirks_bii.json) pushed to a NEW public repository evil-robot/supertruth-connectome-public at publication; this repository stays private as the working record. The paper's code URL points at the public one. | build lead | BII disclosure via git history |
